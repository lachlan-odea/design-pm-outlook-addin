// Placeholder for any UI-less ribbon-button function actions. Currently the
// ribbon button uses ShowTaskpane directly, so nothing is registered here yet.
Office.onReady(() => {
  // intentionally empty
});
